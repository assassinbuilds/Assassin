import pkg from 'pg'; const { Client } = pkg; import { config } from 'dotenv'; import { fileURLToPath } from 'url'; import { dirname, join } from 'path'; const __filename = fileURLToPath(import.meta.url); const __dirname = dirname(__filename); config({ path: join(__dirname, '../.env.local') }); const DATABASE_URL = process.env.DATABASE_URL; const dbClient = new Client({ connectionString: DATABASE_URL, ssl: { rejectUnauthorized: false } }); async function viewUsers() { try { await dbClient.connect(); console.log('✅ Connected to Supabase.\\n'); const { rows } = await dbClient.query('SELECT id, username, first_name, last_name, email, full_name FROM public.profiles'); console.table(rows); } catch (error) { console.error('❌ Error:', error.message); } finally { await dbClient.end(); } } viewUsers();